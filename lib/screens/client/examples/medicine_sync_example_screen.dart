import 'package:flutter/material.dart';

class MedicineSyncExampleScreen extends StatelessWidget {
  const MedicineSyncExampleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Medicine Sync Example')),
      body: const Center(child: Text('Legacy sync example screen stubbed.')),
    );
  }
}
